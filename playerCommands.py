class command (object):
    
    # do the action on the target with values vals
    # this is the default action, a real action should have its own thing
    def act (self, target, vals):
        print "Acting on", target, "with", vals 

class target (object):

    def 
