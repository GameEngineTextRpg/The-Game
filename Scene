class scene(Object): #place where the game will be interacting 

	def __init__(self, args):
		self.args = args #stores all of the arguements in the class

	def action(): #this class will be over written every time
		print "This is the dummy class you should NEVER see this if you did you did something wrong"
